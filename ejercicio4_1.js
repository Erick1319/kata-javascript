// 1. Crea un programa que imprima en consola los números impares del 1 al 50;
//inicializamos contador a 0;
let i=0;
//inicializamos el ciclo hasta 50
for (i=0;i<=50;i++){  
//si se cumple la condición: el modulo de i entre 2 es 1, se va a imprimir  "i" con valores impares.
    if(i%2==1){
        console.log(i);
    }
}

